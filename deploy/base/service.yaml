apiVersion: v1
kind: Service
metadata:
  name: cloudinfer
  labels:
    app: cloudinfer
spec:
  selector:
    app: cloudinfer
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
