# Hello CloudInfer - Local Mock Configuration
# No API keys required. With no backends configured, CloudInfer serves
# built-in mock responses from its OpenAI-compatible endpoint.

server_host: 127.0.0.1
server_port: 8080

# Routing is disabled because this lab uses built-in mock mode.
routing:
  enabled: false
